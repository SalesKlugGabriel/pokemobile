# Godot 4 — cache local do editor, nunca versionar
.godot/

# Build exportada — gerada a partir do código-fonte, não precisa ir pro Git
builds/

# Worktrees de sessões-filha (Claude Code) — metadado de ferramenta, não do jogo
.claude/
